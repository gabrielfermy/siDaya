{
  "name": "@sidaya/database",
  "version": "0.1.0",
  "description": "PostgreSQL database schemas, migrations, and RLS policies for SiDaya",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@sidaya/shared-types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
